CREATE SCHEMA IF NOT EXISTS "QuestGoMail";

SET SCHEMA 'QuestGoMail';

CREATE TABLE IF NOT EXISTS users(
  id       serial primary key,
  name     varchar(256) NOT NULL,
  password varchar(256) NOT NULL
);

INSERT INTO "QuestGoMail".users(name, password)
  VALUES ('Test1', '123');

SELECT * FROM "QuestGoMail".users;
